import(rtracklayer)
importFrom(lattice, barchart)

exportMethods(
              ## gatkio
              plotDepthOfCoverage,
              plotSampleIntervalSummary
              )

export(
       read.DepthOfCoverage,
       ## utils
       print.xtable.booktabs,
       ## 454
       read454.metrics,
       ## picard
       read.dup_metrics,
       read.insert_metrics,
       read.hs_metrics,
       read.algin_metrics
       )
